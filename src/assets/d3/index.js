import { select, selectAll } from 'd3-selection'

import {
  scaleLinear,
  scaleTime,
  scaleOrdinal,
  schemeCategory10
} from 'd3-scale'

import { max, min, deviation } from 'd3-array'

export default {
  select,
  selectAll,
  scaleLinear,
  scaleTime,
  scaleOrdinal,
  schemeCategory10,
  max,
  min,
  deviation
}
